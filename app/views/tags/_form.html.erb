<%= semantic_form_for(@tag, :url => tag_path, :as => :tag) do |form| %>
  <%= form.inputs do %>
    <%= form.input :name, :required => true %>
  <% end %>

  <%= form.buttons do %>
    <%= form.commit_button %> or <%= link_to 'Back', :back %>
  <% end %>
<% end %>
